# Web app
# Base URL of the Go API. Used by lib/api/client.ts.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Development identity headers sent with every API request.
# Values come from dev rows you create locally; do not commit real UUIDs.
NEXT_PUBLIC_TENANT_ID=
NEXT_PUBLIC_USER_ID=
